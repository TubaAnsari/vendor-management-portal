{
  "name": "vendor-portal-backend",
  "version": "1.0.0",
  "description": "Backend API for Vendor Management Portal",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build required for Node.js'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "render-build": "npm install",
    "init-db": "node init-db.js",
    "deploy": "npm run render-build && npm start"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}